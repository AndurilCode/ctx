# compact.md

Token-efficient, lossless Markdown compression for agent pipelines.

## Development

```bash
pnpm install
pnpm test
pnpm build
```

## Planned API

- `compact(markdown, options?)`
- `expand(compactText, options?)`
- `verify(markdown, options?)`
